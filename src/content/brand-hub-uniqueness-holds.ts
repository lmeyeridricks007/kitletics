/**
 * Editorial 43 uniqueness holds — emptied in Fix 56 after family + copy split.
 * Depth-qualified hubs that still fail uniqueness after scrub stay listed here.
 */
export const BRAND_HUB_UNIQUENESS_HOLD_SLUGS = new Set<string>([]);
